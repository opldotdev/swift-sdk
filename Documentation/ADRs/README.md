# Architecture decision records

ADRs capture decisions that affect public API, consensus behavior, dependencies,
security, licensing, or the SwiftPM target graph.

Use `0000-template.md` and never rewrite an accepted ADR to hide a later change;
supersede it with a new record.
